{% extends 'base.html.twig' %}

{% block title %}Calendrier{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {# <link rel="stylesheet" href="../css/style.css"> #}

    {# <link rel="shortcut icon" href="../icons/fav_icon.png" type="image/x-icon"> #}
    <title>Calendrier</title>
</head>
<body>
  
    <main>
        <div class="welcome">
            {% if app.user %}            
            <a href="{{path('app_event_index')}}">
            <div class="wel_choise">
                <p>Consulter</p>
                <img src="{{asset('img/consult.png')}}" alt="consuler">
            </div>
            </a>
            {% else %}
                <a href="{{path('app_login')}}">
            <div class="wel_choise">
                <p>Consulter</p>
                <img src="{{asset('img/consult.png')}}" alt="consuler">
            </div>
            </a>
            {% endif %}
            {% if app.user %}
            
            <a href="{{path('app_event_new')}}">
            <div class="wel_choise">
                <p>Créer un événement</p>
                <img src="{{asset('img/create_event.png')}}" alt="créer un événement">
            </div>
            </a>
            {% else %}
              <a href="{{path('app_login')}}">
            <div class="wel_choise">
                <p>Créer un événement</p>
                <img src="{{asset('img/create_event.png')}}" alt="créer un événement">
            </div>
            </a>
            {% endif %}
        </div>
    </main>
    
</body>
</html>
{% endblock %}
